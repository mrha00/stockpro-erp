# StockPro 进销存系统 — 简历就绪检查清单

> **项目定位**：简历展示项目，不涉及实际上线部署。重点是 GitHub 仓库的专业性、代码质量和可讲解性。

---

## 文档说明

| 文档 | 用途 |
|------|------|
| **本清单** | 可勾选的验收标准、当前进度、完成底线 |
| [StockPro简历项目完善流程.md](./StockPro简历项目完善流程.md) | 实施步骤、示例代码、面试 Q&A、演示脚本 |
| [生产上线指南.md](./生产上线指南.md) | 真实上线参考；**简历项目不必逐项落地**，面试可讲思路即可 |

**简历描述模板**见完善流程 [§10 简历描述模板](./StockPro简历项目完善流程.md#10-简历描述模板)，本清单不再重复维护。

---

## 当前仓库快照

> 每次推进后更新下表，避免清单与仓库脱节。最后核对：**2026-05-30**

| 模块 | 状态 | 备注 |
|------|------|------|
| `appsettings.json` 占位符 | ✅ | 使用 `CHANGE_ME` |
| `appsettings.Local.json.example` | ✅ | 位于 `InventorySystem.Api/` |
| `appsettings.Local.json` 未入库 | ✅ | 已在 `.gitignore` |
| 根目录 `.gitignore` | ✅ | 已放开 `docs/` |
| 根目录 `README.md` | ✅ | Mermaid/徽章/截图/账号已就绪 |
| `LICENSE` 文件 | ✅ | MIT |
| `docker-compose.yml` | ✅ | sqlserver + api + frontend |
| 前后端 `Dockerfile` | ✅ | 已创建 |
| GitHub Actions CI | ✅ | 前后端分 job + build + test |
| 后端单元测试 | ✅ | Auth / Inventory / SalesOrder + 既有测试 |
| 前端 Vitest | ✅ | Login + inventory 工具 |
| `/health` 端点 | ✅ | `/health`、`/health/ready` |
| Serilog / Swagger(Dev) | ✅ | `Program.cs` 已配置 |

图例：✅ 已完成 · 🔄 部分完成 · ❌ 未开始 · ⚠️ 需自检

---

## 一、必须完成（简历硬性要求）

### 1.1 移除硬编码敏感信息

**目的**：避免在 GitHub 上暴露密码，显得不专业。  
**怎么做**：见完善流程 [§4.1](./StockPro简历项目完善流程.md#41-移除硬编码敏感信息)。

- [x] `appsettings.json` 中连接串、JWT 密钥为占位符（如 `CHANGE_ME`）
- [x] 存在 `InventorySystem/src/InventorySystem.Api/appsettings.Local.json.example`
- [ ] 本地创建 `appsettings.Local.json`（仅本机，不提交）
- [x] 确认 `appsettings.Local.json` 已被 `.gitignore` 且未被 Git 跟踪
- [x] 根目录 `README.md` 有「配置本地环境」说明，路径与 example 一致
- [ ] （可选）抽查历史提交无真实密码：`git log -S "Password=" --oneline`

### 1.2 更新 .gitignore

**目的**：文档、种子脚本可提交；敏感文件仍被忽略。

**建议修改**（根目录 `.gitignore`）：

```gitignore
# 删除以下规则（当前会阻止文档入库）：
# docs/
# **/docs/
# *.sql

# 若需提交种子脚本，可改为仅忽略备份类 SQL，例如：
# **/backups/*.sql
# !InventorySystem/scripts/SeedData.sql
```

- [x] 移除 `docs/`、`**/docs/` 忽略规则
- [x] 调整 `*.sql`：改为仅忽略 `**/backups/*.sql`
- [x] 确认仍忽略：`.env`、`appsettings.Local.json`、`**/appsettings.Local.json`
- [x] 确认 `bin/`、`obj/` 在忽略列表

### 1.3 完善 README.md（仓库根目录）

**目的**：GitHub 首页第一印象。

- [x] 项目简介（一句话）
- [x] 特性列表（6–8 项）
- [x] 技术栈说明
- [x] 快速开始（本地 / Docker 说明）
- [x] Mermaid 或架构示意图
- [x] 徽章（.NET、React、License 等）
- [x] 界面截图 2–3 张（`InventorySystem/docs/images/`）
- [x] 文档链接指向 `InventorySystem/docs/`
- [x] 添加根目录 `LICENSE` 文件
- [x] **演示账号与种子数据一致**（见 §1.5）

### 1.4 GitHub Actions CI

**目的**：主分支展示绿勾，体现工程规范。  
**怎么做**：见完善流程 [§8.1](./StockPro简历项目完善流程.md#81-github-actions-工作流)。

**最低线（简历可接受）**

- [x] 后端 `dotnet restore` / `build` / `test`
- [x] 前端 `npm ci` + 类型检查（`npm run lint`）
- [x] 前端 `npm run build` 纳入 CI
- [x] 提交并锁定 `stockpro-erp/package-lock.json`

**加分线**

- [ ] 前后端分 job 并行
- [ ] 测试覆盖率 artifact（Coverlet / Vitest coverage）
- [ ] （有 Dockerfile 后）镜像构建与健康检查 smoke test

### 1.5 演示与文档一致性（易忽略，建议必做）

**目的**：演示、README、种子账号一处维护，避免面试现场登录失败。

**标准演示账号**（与完善流程一致，密码统一 `123456`）：

| 角色 | 用户名 |
|------|--------|
| 管理员 | admin |
| 采购员 | purchase |
| 销售员 | sale |
| 仓管员 | stock |

- [x] 根目录 `README.md` 默认账号表与上表一致
- [x] 种子数据通过 `DatabaseSeeder` + `SeedData:Enabled` 灌入
- [x] `SeedData:Enabled` 仅在开发环境且显式开启时执行
- [x] 前端 `stockpro-erp/.env.example` 已更新

### 1.6 Git 仓库卫生（公开前自检）

- [ ] 无 `.env`、`appsettings.Local.json` 被 track
- [ ] 提交信息可读（`feat`/`fix`/`docs` 等，见完善流程 §13.3）
- [ ] 功能按提交拆分，历史能体现迭代过程

---

## 二、强烈推荐（加分项）

### 2.1 Docker 容器化

**目的**：展示 DevOps 能力。  
**怎么做**：见完善流程 [§6](./StockPro简历项目完善流程.md#6-阶段三容器化与部署配置)。

- [x] 后端 `InventorySystem/Dockerfile`（多阶段构建）
- [x] 前端 `stockpro-erp/Dockerfile` + `nginx.conf`
- [x] 根目录 `docker-compose.yml` 包含 sqlserver + api + frontend
- [x] README 补充「Docker 一键启动」与访问地址

### 2.2 后端单元测试

**目的**：核心业务有可讲的测试用例。  
**怎么做**：见完善流程 [§5.2](./StockPro简历项目完善流程.md#52-单元测试示例)。

**已有（勿重复造轮子）**

- [x] `CategoryServiceTests`
- [x] `ProductServiceTests`

**建议补齐**（每个 Service ≥3 个用例即可，不必追求 70% 覆盖率）

- [x] `AuthService`：合法登录、错误密码、刷新令牌
- [x] `InventoryService`：入库、出库、库存不足
- [x] `SalesOrderService`：创建订单、审批、校验

### 2.3 前端单元测试

**目的**：体现全栈质量意识。

- [x] 安装 Vitest + React Testing Library，`npm run test`
- [x] `Login` 组件 3 个用例
- [x] `src/utils/inventory.ts` 工具函数测试

### 2.4 健康检查端点

**目的**：与 Docker / 面试中的「可观测性」话术衔接。

- [x] `GET /health`（存活）
- [x] `GET /health/ready`（含数据库检查）
- [ ] （可选）Dockerfile `HEALTHCHECK` 需镜像内 curl/wget

---

## 三、可选增强（锦上添花）

### 3.1 安全响应头

- [x] `X-Content-Type-Options: nosniff`
- [x] `X-Frame-Options: DENY`  
见完善流程 [§4.4](./StockPro简历项目完善流程.md#44-添加安全头)。

### 3.2 Swagger 文档

**现状**：开发环境已启用 Swagger。本阶段为**验证与补全**，非从零搭建。

- [ ] 主要 Controller 有摘要/描述
- [ ] 关键 DTO 有示例或注释
- [ ] 确认生产环境不暴露 Swagger UI

### 3.3 结构化日志

**现状**：Serilog 已接入 `Program.cs` 与 `appsettings.json`。

- [ ] 核对日志级别、文件滚动配置
- [ ] （可选）请求日志中间件或审计中间件行为符合演示需要

---

## 四、不需要做的事情

以下属于真实上线范畴，**简历展示项目可跳过**（面试讲思路即可）：

- ~~生产环境 CORS 白名单收紧到单一域名~~
- ~~Prometheus + Grafana 完整落地~~
- ~~分布式追踪（OpenTelemetry 全链路）~~
- ~~按《生产上线指南》全量部署与运维~~
- ~~SSL 证书、负载均衡、数据库备份策略~~

保留《生产上线指南》作**谈资**，不必按清单全部实现。

---

## 五、执行顺序建议

### 时间紧？最小路径（约 1 天）

1. §1.2 `.gitignore` + 文档可提交  
2. §1.1 / §1.5 敏感信息与演示账号一致  
3. §1.3 README 修正链接与账号（截图可后补）  
4. §1.4 CI 确保 `build` + 主分支绿勾  

### 推荐四天节奏

| 天 | 内容 |
|----|------|
| **第 0 天（优先）** | §1.2、§1.5、§1.6 — 仓库可安全公开 |
| **第 1 天** | §1.1 收尾、§1.3 README、§1.6 |
| **第 2 天** | §2.1 Docker、§1.4 CI 加分项；**傍晚**本地演示跑通（不依赖 Docker 成功） |
| **第 3 天** | §2.2、§2.3 测试补齐 |
| **第 4 天** | §2.4 健康检查、§3 可选项、完善流程 §12 演示彩排 |

---

## 六、完成标准（可写进简历的底线）

### A. 仓库可公开

- [x] §1.1、§1.2、§1.6 全部勾选
- [x] 存在 `LICENSE`，文档可被 clone 后阅读

### B. 可演示

- [ ] 本地 **或** Docker 一种方式，15 分钟内跑通主流程（需本机验证）
- [x] §1.5 演示账号与种子一致（admin / 123456）
- [ ] 能流畅讲解 5–10 分钟（商品 → 库存 → 订单 → 仪表盘）

### C. 工程印象

- [x] §1.4 最低线 CI 通过（推送后查看 GitHub Actions）
- [x] §2.2 核心 Service 测试已补齐
- [x] README 含 Mermaid 架构图

### D. 简历表述一致

- [x] 桌面 `简历.txt` 已添加本项目（Vibe Coding）；表述与仓库能力一致

---

## 七、与完善流程的差异（速查）

| 主题 | 本清单 | 完善流程 |
|------|--------|----------|
| 粒度 | 勾选验收 | 详细步骤 + 代码示例 |
| 覆盖率 | 每 Service 若干用例 | 可写 70%+ 为理想目标 |
| 面试 / 演示 | 仅完成标准 | §11 Q&A、§12 演示脚本 |
| GitHub / 提交规范 | §1.6 摘要 | §13 全文 |

---

**文档版本**：v1.1  
**创建日期**：2026-05-30  
**最后更新**：2026-05-30  
**适用场景**：简历项目展示、公开仓库前自检
